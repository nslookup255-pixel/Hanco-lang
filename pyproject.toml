[project]
name = "hanco"
version = "0.8.3"
description = "읽히는 코드, 한국어 프로그래밍 언어 한코"
authors = [
    {name = "nohha"}
]
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
hanco = "hanco.cli.hanco:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
